<li><%= link_to tag.name, articles_by_tag_path(tag.name) %><%= link_to task, remote: true, method: :delete,  data: { confirm: 'Are you sure?' } do %>
  <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
<% end %>
</li>